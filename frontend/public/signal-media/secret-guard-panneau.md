# Secret Guard — panneau de la barre d'état

- Capture produite le 16 septembre 2026 à partir du code du dépôt : c'est le rendu du vrai HTML retourné par `statusTooltipMarkdown()` (`secret-guard/packages/vscode/src/status-tooltip.ts`), pas une maquette ni une illustration.
- L'image montre l'infobulle de survol de l'élément de barre d'état, avec ses quatre sections numérotées : `01 NIVEAU DE PROTECTION`, `02 ASSISTANTS COUVERTS`, `03 VÉRIFIER AVANT DE PARTAGER`, `04 PASSERELLE D'ENTREPRISE`, en-tête « XSOM / Secret Guard · Prêt à veiller » et pied « Analyse locale · Sans télémétrie · xSOM ».
- État exact reproduit : santé `active` (« Prêt à veiller »), mode `observe` (« Avertir et laisser passer », exposition Élevée, jauge à quatre segments), trois hôtes configurés — Claude Code, Codex avec « Configuré · approbation du hook à confirmer » et son bouton « Finaliser », Windsurf Cascade —, passerelle « Non connecté » avec « Raccorder », aucun échec d'application de mode et aucune analyse récente. Sous l'infobulle, la barre d'état porte l'élément `$(shield) Secret Guard · 👁️ Avertir et laisser passer` avec le fond d'alerte imposé par le mode permissif, et la cloche des notifications à droite. Aucune donnée réelle, aucun secret, aucun jeton, aucun nom de personne.
- Palette : celle du thème VS Code Dark Modern, non inventée — infobulle `#202020`, bordure `#CCCCCC` à 20 %, texte `#CCCCCC`, descriptions `#9D9D9D`, aplats `#2B2B2B`, bleu `#59a4f9`, rouge `#F14C4C`, vert `#89D185`, jaune `#CCA700`, bouton `#0078D4`, liens `#4daafc`, barre d'état `#181818` sur fond d'éditeur `#1F1F1F`, pastille de mode `#7A6400` sur texte blanc.
- Génération locale : chaîne markdown réelle → conversion des icônes de thème `$(nom)` en `<span class="codicon codicon-nom">` → contrôle contre l'allowlist réelle du sanitizer de VS Code (balises, attributs, `style` de `<span>`) → insertion dans le DOM réel du survol (`.monaco-hover.workbench-hover` › `.monaco-hover-content` › `.hover-row.markdown-hover` › `.hover-contents` › `.rendered-markdown`), avec la feuille de style `workbench.desktop.main.css` et la police `codicon.ttf` du build VS Code 1.137.0, les variables de couleur de Dark Modern et « Noto Color Emoji » pour l'emoji. Capture Chromium via Playwright, `deviceScaleFactor` 2, `colorScheme` sombre.
- `secret-guard-panneau.png` : 958 × 1 110 pixels (composition de 479 × 555 pixels CSS capturée à l'échelle 2), 153 046 octets, fond `#1F1F1F` avec une marge de 26 pixels autour du panneau et de la barre d'état.
- Vérification avant capture : fond calculé `rgb(32, 32, 32)`, bordure `1px solid rgba(204, 204, 204, 0.2)`, rayon 5 px, police 13 px / 19 px, contenu plafonné à 500 px avec un remplissage de 4 px 8 px et aucun débordement (`scrollWidth` = `clientWidth` = 423), barre d'état de 22 px en 12 px sur `#181818`. Les sept codicons employés (`shield`, `settings-gear`, `clippy`, `file-text`, `plug`, `check`, `bell`) sont résolus par la police codicon, largeur de glyphe distincte de celle du repli : aucun carré tofu. Aucun message de console, aucune violation CSP, aucune requête en échec.
- Écart connu : la pile de polices de VS Code sous Linux (`system-ui, Ubuntu, Droid Sans, sans-serif`) se résout ici sur DejaVu Sans ; sur un poste équipé d'Ubuntu ou de Segoe UI, le texte est un peu plus étroit et le panneau, plafonné à 500 px, se resserre d'autant.
