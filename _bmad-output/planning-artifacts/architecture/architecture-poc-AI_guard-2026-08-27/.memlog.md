---
scope: xSOM AI Guard — strate v2.5, colonne vertébrale d'architecture
purpose: Fixer les invariants que les 45 exigences FR-153..197 doivent partager, au-dessus de ARCHITECTURE-V2.md sans le remplacer
altitude: strate (initiative) → epics
updated: 2026-08-27T15:04
---

- (direction by user) Mode coaching, mais sans blocage : le propriétaire mène en parallèle une recherche pour un plan de maquette solide avant de coder. Je pose chaque décision porteuse avec ses alternatives et mon inclinaison ; il réagit de façon asynchrone.
- (constraint by user) Le propriétaire a été DÉÇU des epics et stories produits par BMAD auparavant. Conséquence pour cette colonne vertébrale : elle doit être plus prescriptive que la moyenne — plus elle fixe d'invariants, moins la couche story a de latitude pour dériver vers du générique.
- (direction by user) Quatre livrables retenus : la colonne vertébrale seule (substrat de build), un schéma d'architecture souveraine (public RSSI + page de présentation), un document de design lisible (public intégrateur), et une vue du découpage (pilotage).
- (constraint) RÉALITÉ DU CODE : aucun module planifié par ARCHITECTURE-V2 n'existe (core/pipeline.py, provenance.py, control.py, sessions.py, taint_store.py absents). La séquence de gardes vit en ligne dans gateway/server.py:call_tool.
- (constraint) DIVERGENCE LIVE ENTRE CHEMINS D'INGESTION : MCP exécute 7 gardes (integrity, RBAC, policy, juge, risque, taint, HITL) ; HTTP/SDK via core/decision.py n'en exécute que 4 (policy, juge, risque/trust, HITL). Integrity, RBAC et taint sont ABSENTS du chemin HTTP. Un agent sur /v1/authorize peut appeler un outil en quarantaine, contourne le RBAC par outil, et n'est jamais marqué par le taint.
- (question) Conséquence à porter dans la PRD : les lignes Bloqué de M-02, M-11 et M-12 ne sont vraies que sur MCP. SM-12 (« 6 bloquées sur 9 ») doit être qualifié par chemin d'ingestion sous peine d'être faux.
- (decision) AD-1 [PROPOSÉ] Un seul pipeline, un seul pli. Chaque adaptateur d'ingestion construit un CallContext et appelle un unique evaluate_call ; les gardes renvoient un palier MINIMUM, le pipeline plie par max sur l'ordre des paliers. Binds: tous les gardes, tous les adaptateurs. Prevents: deux chemins appliquant des jeux de gardes différents (défaut live), et tout garde futur abaissant un verdict antérieur — FR-170 n'est exprimable qu'ainsi.
- (decision) AD-2 [PROPOSÉ] La frontière de provenance appartient à l'adaptateur. enforcement_mode, tenant_id et gateway_token_id sont posés par l'adaptateur depuis l'état authentifié ; tout champ fourni par l'appelant vit sous un espace 'declared' et s'affiche comme déclaré. Binds: écrivains d'audit, rendu d'export. Prevents: la partie supervisée renseignant ses propres champs d'audit (INV-2, INV-3).
- (decision) AD-3 [PROPOSÉ] Un seul vocabulaire de contraintes, un seul évaluateur. Contraintes et prédicats d'arguments sont un mécanisme unique, fermé, versionné et typé ; la classification des exécuteurs s'en sert. Clé inconnue = document rejeté au chargement. Prevents: la situation actuelle où allowed_clients est évalué dans gateway/server.py et allowed_domains dans core/policy.py — source même du fail-open EXH-2.
- (decision) AD-4 [PROPOSÉ] Une seule sortie réseau. Toute récupération d'URL fournie par un tenant passe par un helper unique. Binds: sinks, notifications, ancres. Prevents: la SSRF réintroduite fonctionnalité par fonctionnalité (INV-7).
- (decision) AD-5 [PROPOSÉ] La souveraineté est une propriété vérifiée en CI, pas une promesse. Le chemin de décision porte une allowlist d'hôtes sortants ; un test échoue si un module du chemin de décision peut joindre autre chose. Prevents: une fonctionnalité future ajoutant discrètement un appel hors UE — rend FR-176 structurel au lieu d'aspirationnel.
- (decision) AD-6 [PROPOSÉ] Les scénarios sont des assertions, les vidéos n'en sont que le rendu. Toute ligne revendiquée Bloqué possède un script de scénario exécuté en CI. Prevents: la revendication qui dérive du réel — donne un mécanisme à la contre-métrique CM-7.
- (decision) AD-7 [PROPOSÉ] Le mode observation est une branche unique au point de pli, pas un mode par chemin. Prevents: un monitor implémenté différemment selon l'ingestion.
- (decision) AD-8 [PROPOSÉ] Toute revendication de couverture est qualifiée par chemin d'ingestion. La carte indique MCP (obligatoire) ou coopératif par ligne. Prevents: le « 6 bloquées sur 9 » énoncé sans qualification, aujourd'hui faux sur HTTP.
- (decision) AD-9 [PROPOSÉ] Le cœur d'enforcement est prouvable sans console ni plan d'identité. Les scénarios tournent en headless contre une base et le gateway, exactement comme scripts/demo.py le fait déjà (Postgres éphémère, migrations appliquées, aucun service d'auth). Binds: contrat de scénario, CI. Prevents: la strate bloquée par les onze défauts DEP-1..11 du plan d'identité et de déploiement — qui ne touchent pas le chemin de décision.
- (decision) AD-10 [PROPOSÉ] La carte de couverture publiée est GÉNÉRÉE à partir des scénarios qui passent, jamais maintenue à la main. Une ligne ne peut être revendiquée Bloqué que si un scénario le prouve en CI. Binds: carte publiée, scénarios, supports commerciaux. Prevents: toute revendication non adossée à un test — rend la contre-métrique CM-7 structurellement nulle au lieu de la confier à la vigilance.
- (decision) AD-11 [PROPOSÉ] Le tenant de démonstration est une graine déterministe committée, pas un script de génération. Conséquence : « aucune donnée personnelle de tiers » se vérifie en lisant le dépôt, pas en faisant confiance à un générateur. Un contrôle CI échoue si la graine contient une forme de donnée personnelle réelle. Binds: FR-182. Prevents: une fuite de PII réelle dans un artefact public, sur un produit de gouvernance de l'IA.
- (decision) AD-12 [PROPOSÉ] Le chemin de décision n'exige qu'un Postgres contrôlé par l'opérateur. Tout besoin de superutilisateur (BYPASSRLS, DEP-7) est un prérequis DBA documenté et détecté au démarrage, jamais une hypothèse silencieuse. Binds: enveloppe de déploiement, prestation chez le client, profil P4. Prevents: une installation qui se croit saine sur un Postgres managé où l'isolation ne s'applique pas.
- (question) DIFFÉRÉ — emplacement du modèle de profil (FR-172). Attribut de tenant en base (couverture calculée) ou artefact de mission (couverture rédigée) ? Dépend de QO-7, non tranchée. Ne pas décider ici : les deux réponses produisent des architectures différentes et le choix est commercial, pas technique.
- (question) EN ATTENTE DE RATIFICATION — amplitude de l'extraction du pipeline. Inclinaison consignée : extraction minimale (evaluate_call + CallContext) conçue pour que l'extraction complète v2 (core/pipeline.py + core/provenance.py) soit un élargissement et non une réécriture. Réversible.
- (change) CORRECTION DE NUMÉROTATION. ARCHITECTURE-V2.md porte déjà AD-1 à AD-20 : mes douze décisions collisionnaient intégralement. Elles sont renumérotées à partir de AD-21, comme les exigences démarrent à FR-153. Correspondance : AD-1→AD-21 (un pipeline, un pli), AD-2→AD-22 (frontière de provenance), AD-3→AD-23 (vocabulaire de contraintes), AD-4→AD-24 (une sortie réseau), AD-5→AD-25 (souveraineté en CI), AD-6→AD-26 (scénarios = assertions), AD-7→AD-27 (mode observation), AD-8→AD-28 (couverture qualifiée par chemin), AD-9→AD-29 (prouvable headless), AD-10→AD-30 (carte générée), AD-11→AD-31 (graine déterministe), AD-12→AD-32 (Postgres opérateur). Les AD-1..20 du parent sont hérités, contraignants et en lecture seule — jamais renumérotés.
- (constraint) HÉRITÉ ET CONTRAIGNANT — AD-4 du parent tranche DÉJÀ « un seul pipeline » : extraire core/pipeline.py que les deux chemins appellent, plutôt que recopier les gardes manquants dans core/decision.py, au motif que « la cause racine est la duplication, pas l'omission ». Mon AD-21 tel que formulé le redisait à l'identique. Ce n'est pas ma décision à prendre.
- (change) AD-21 REFORMULÉE, plus étroite et plus utile. Le parent AD-4 fixe l'existence d'un pipeline unique mais laisse ouverte la MÉCANIQUE DU PLI : il se contente d'un corpus de décisions de référence, que INV-13 juge précisément insuffisant (« un corpus prouve que les entrées d'aujourd'hui donnent les sorties d'aujourd'hui ; il ne dit rien d'un garde futur »). AD-21 devient donc : les gardes renvoient un palier MINIMUM, le pipeline plie par max sur l'ordre des paliers, et un test de propriété vérifie qu'aucun verdict intermédiaire ne peut être abaissé. C'est ce que le parent a laissé ouvert.
- (change) TAINT — amendement, pas décision neuve. Le parent AD-10 tranche l'emplacement (table Postgres avec RLS, session non résoluble = teintée, échec de lecture = refus sur irréversible/external_send) mais pas la CLÉ. INV-4 montre que primary key (tenant_id, session_id) est contournable puisque le session_id est déclaré par l'agent. FR-154 amende donc AD-10 en ajoutant gateway_token_id, sans contredire son choix d'emplacement. À porter comme amendement explicite d'un AD hérité.
- (change) AD-32 est aussi un amendement : le parent AD-6 tranche Postgres externe vs embarqué ; mon apport se limite au prérequis superutilisateur (BYPASSRLS, DEP-7) détecté au démarrage. Les dix autres (AD-22..AD-31) sont bien neuves — le parent ne couvre ni la SSRF, ni le mode observation, ni les scénarios assertés, ni la souveraineté vérifiée en CI, ni le vocabulaire de contraintes.
- (event) REVUE ADVERSARIALE : 11 trous. Les trois plus graves vérifiés dans le code, tous confirmés.
- (change) AD-21 EST LOGIQUEMENT FAUSSE, et c'est mon erreur. Un pli par max ne peut renvoyer qu'une valeur qu'un filtre a produite : il ne peut pas INJECTER un plancher. Or ToolRule (core/policy.py:98-113) ne contraint jamais approval contre action_class — « class: irreversible, approval: auto » est un document valide. Le test de propriété que j'ai spécifié (« irreversible ne résout jamais en auto ») est donc insatisfiable par le mécanisme que j'ai spécifié. Correctif : un étage class_floor nommé, inconditionnel, ordonné après policy et juge.
- (change) « Aucun filtre ne dépend d'un autre » (ma colonne vertébrale) est FAUX. taint lit action_class, risk lit action_class et decision, judge ne se déclenche que sur ambiguous, rbac a besoin de la résolution de nom. La chaîne est un ordre PARTIEL et il doit être publié — sans quoi deux équipes ordonnent halt différemment, passent toutes deux le test de propriété (max est commutatif) et rendent des verdicts différents.
- (change) AD-25 SE CONTREDIT EN TROIS ENDROITS, comme je le soupçonnais : la chaîne liste « judge (classify only) » comme filtre, la convention dit qu'un filtre « n'appelle jamais de modèle », et AD-25 exige zéro sortie réseau. Le juge EST un appel réseau sur le chemin de décision. À trancher : le juge sort de la chaîne pliée (enrichisseur pré-pli, exempté et profilé), ou AD-25 s'assouplit en « aucune sortie hors de la frontière déclarée par l'opérateur » et FR-176 devient « capable de fonctionner hors ligne, juge désactivé ».
- (constraint) DÉFAUT VIVANT NOUVEAU, hors des 34 et des 21 écarts. .env.example:117-119 affirme : « Empty MISTRAL_API_KEY disables the judge (fail-closed): ambiguous tools are treated as irreversible, so they go to human approval instead of being auto-allowed. » Le code fait l'inverse : sans juge, core/policy.py:295 renvoie l'approval déclaré de la règle avec action_class=None, et l'escalade est court-circuitée par « judge is not None » (gateway/server.py:149, core/decision.py:94). La garantie documentée n'existe pas. L'exemple livré dans SPEC.md:200 déclare human_in_the_loop et ne tombe donc pas dans le trou — mais la sûreté tient à la convention de l'exemple, pas à la construction. Clé vide = configuration par défaut.
- (constraint) DÉFAUT VIVANT NOUVEAU : sur la branche classify: ambiguous, core/policy.py:295 RETOURNE AVANT _constraints_ok. Les contraintes d'une règle ambiguë ne sont donc JAMAIS évaluées. C'est le jumeau moteur de la contrainte décorative de scripts/demo.py, et SPEC.md:197 livre justement un exemple portant dry_run et business_hours_only — deux contraintes sans effet.
